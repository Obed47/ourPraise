import React from 'react'
import './titleAndText.css'
export default function Title() {
  return (
    <div>
        <h1>Acapella Music App</h1>
        <p>The set of n × n orthogonal matrices, under multiplication,
             forms the group O(n), known as the orthogonal group. The 
             subgroup SO(n) consisting of orthogonal matrices with determinant 
             +1 is called the special orthogonal group, and each of its elements 
             is a special orthogonal matrix. As a linear transformation, every special 
             orthogonal matrix acts as a rotation.</p>
    </div>
  )
}
